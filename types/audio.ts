export type Metadata = {
  videoUrl: string;
  album: string;
  artist: string;
  category: string;
  filename: string;
  thumbnail: string;
  title: string;
};

export type DownloadFile = {
  outputUrl: string;
};
